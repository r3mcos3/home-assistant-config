---
- id: system_mqtt_clock
  alias: System - Mqtt Clock
  trigger:
    platform: mqtt
    topic: alarm_clock_mqtt/start
    payload: "alarming"
  action:
    - service: light.turn_on
      entity_id:
        - light.closet_light
        - light.kitchen_spots
